import { TemplateRefsDirective } from './template-refs.directive';

describe('TemplateRefsDirective', () => {
  it('should create an instance', () => {
    const directive = new TemplateRefsDirective();
    expect(directive).toBeTruthy();
  });
});
